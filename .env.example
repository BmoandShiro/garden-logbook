# Example .env for Garden Logbook
# Copy this file to .env and fill in your own values.

# Database
DATABASE_URL="postgresql://garden_user:garden_password@localhost:5432/garden_logbook?schema=public"

# NextAuth secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="your-random-secret"

# ---
# 1. Google/Discord OAuth (for your own Tailscale/private use)
# Set your live domain here (e.g., https://your-tailscale-domain.ts.net)
NEXTAUTH_URL=https://your-tailscale-domain.ts.net

# Google OAuth credentials (optional, for Google sign-in)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Discord OAuth credentials (optional, for Discord sign-in)
DISCORD_CLIENT_ID=your-discord-client-id
DISCORD_CLIENT_SECRET=your-discord-client-secret

# ---
# 2. Magic Link (Email) Sign-In (works for all self-hosters)
# Configure your SMTP/email settings below
EMAIL_SERVER_HOST=smtp.example.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your@email.com
EMAIL_SERVER_PASSWORD=your-email-password
EMAIL_FROM=your@email.com

# ---
# Only commit this .env.example file. Never commit your real .env with secrets! 